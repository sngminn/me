# 🧠 Implementation Plan: Blog UI & Navigation (Issue 31)

**목표**: 블로그의 기본 골격을 갖추고, 글 목록과 상세 내용을 볼 수 있는 UI를 구현한다. 특히 **옵시디언 스타일의 내부 링크 이동**이 자연스럽게 동작해야 한다.

---

## 1. 레이아웃 설계 (Layout Design)

사용자 요청에 따라 **좌측 사이드바**를 포함한 2단 레이아웃을 구성합니다.

### 구조
```
+----------------+------------------------------------------------+
|  Sidebar       |  Main Content                                  |
|                |                                                |
|  [Post List]   |  # Post Title                                  |
|  - Post A      |                                                |
|  - Post B      |  Content... [[Internal Link]] ...              |
|                |                                                |
|  [Related]     |                                                |
|  - Linked Post |                                                |
+----------------+------------------------------------------------+
```

*   **Sidebar**:
    *   **All Posts**: 전체 글 목록 (최신순).
    *   **Related Posts**: 현재 보고 있는 글과 연결된 글 목록 (상세 페이지 진입 시 표시).
*   **Main Content**:
    *   MDX로 렌더링된 본문.
    *   깔끔한 타이포그래피 (`@tailwindcss/typography` 플러그인 사용 고려).

---

## 2. 핵심 기능 구현 (Core Features)

### A. WikiLink 변환 (WikiLink Transformation)
`next-mdx-remote`는 기본적으로 `[[Link]]` 문법을 지원하지 않습니다.
따라서 MDX로 변환하기 **전에** 정규식을 사용하여 표준 마크다운 링크로 바꿔줍니다.

*   **변환 규칙**:
    *   `[[slug]]` -> `[slug](/posts/slug)`
    *   `[[slug|text]]` -> `[text](/posts/slug)`
*   **구현 위치**: `src/lib/obsidian/post.ts`의 `getPostBySlug` 함수 내부.

### B. 동적 라우팅 (Dynamic Routing)
*   **경로**: `/app/posts/[slug]/page.tsx`
*   **기능**:
    1.  URL의 `slug`로 포스트 데이터를 가져옴.
    2.  없으면 404 표시.
    3.  있으면 `PostLayout`에 태워서 렌더링.

### C. 사이드바 컴포넌트 (`Sidebar.tsx`)
*   **Props**: `posts` (전체 목록), `relatedPosts` (선택적).
*   **스타일**: `sticky` 포지셔닝으로 스크롤 시에도 따라오게 설정.

---

## 3. 파일 구조 변경
```diff
 src/
   components/
     layout/
+      Sidebar.tsx      (신규)
+      PostLayout.tsx   (신규)
   lib/
     obsidian/
       post.ts          (수정: WikiLink 변환 로직 추가)
 app/
   posts/
     [slug]/
+      page.tsx         (신규)
```

---

## 4. 검증 계획
1.  `/posts/beginning` 접속 시 내용이 잘 보이는가?
2.  본문의 `[[process]]` 링크가 파란색으로 변하고, 클릭 시 `/posts/process`로 이동하는가?
3.  좌측 사이드바에 글 목록이 뜨는가?
