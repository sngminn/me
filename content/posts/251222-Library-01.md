---
title: 인생 첫 라이브러리 만들기 - 01
date: 2025-12-22
updated: 2025-12-22
tags:
  - NPM_라이브러리_개발일기
published: false
aliases: []
---
AI 쓰면서 열받는 거

이것은 **'중요'**합니다.

이것은 **'중요'** 합니다.

**'중요'** 합니다.

이것은 **'중요'**하고 **'필수'**적인 항목입니다.

이것은 **'중요'** 하고 **'필수'** 적인 항목입니다.

This is **Important**123 test.

```typescript
  it("문장 끝에 Bold가 올 경우 공백 추가 안 함", () => {
    const input = "이것은 **중요**";
    expect(fixMarkdown(input)).toBe(input);
  });

  it("조사가 바로 붙는 경우 공백 분리하지 않음 (한글 문법 준수)", () => {
    const input = "**리액트**는 훌륭합니다.";
    expect(fixMarkdown(input)).toBe(input);
  });

  it("특수문자로 끝나는 Bold 뒤에 한글이 오면 공백 추가", () => {
    const input = "**와우!**정말?";
    const expected = "**와우!** 정말?";
    expect(fixMarkdown(input)).toBe(expected);
  });
});
```


